56830ec9dde2e3ce94525e84beab47108e45561d
=== GOLDEN MASTER baseline captured 2026-09-11T20:41:48Z ===
FORMAT  weeks 1-13, 7-team playoff, seeding division-winners-first, bracket reseeds  (from the espn format block)
BACKTEST age-curve opportunity FULL-SYSTEM(real lineup)+inflation no-lookahead(prev-yr proj)  16-team $200 HALF 7-team-playoff | reserve=4 maxShare=0.25  market 0.3  book vor
  CHAMPIONSHIPS: 38.5%  (random 6.3%)  |  playoffs: 96%
  TESTS baseline: 736 total, 733 pass, 1 fail (pre-existing)

=== GOLDEN MASTER MOVED 2026-09-12: FFToday consensus blend SHIPPED ===
The redesign's cleanup phases (1-2) were behavior-preserving and golden-master-gated at 38.5%.
Phase 3 then VALIDATED a value change (the FFToday consensus blend, consensusBlend lever) through the
CPCV arbiter, and it is now SHIPPED ON by default. The flagless golden master is therefore now:
  CHAMPIONSHIPS: 41.3%  (random 6.3%)  |  playoffs: 97%   (n=150, 1999-2024)
  `--consensus-blend 0` reproduces the pre-edge 38.5% / 96% base; the +2.8pp between them is the edge.
This is a DELIBERATE value change, not drift -- the 38.5% above is retained as the pre-edge reference.

=== GOLDEN MASTER MOVED 2026-09-12 (2): bench-discount RE-OPTIMISED 0.25 -> 0.35, SHIPPED ===
The lever re-optimisation (docs/redesign/power-and-surrogate.md) found bench-discount's optimum had
moved to ~0.35 (value bench-only players a bit more): +1.09pp titles, CI [0.16,2.11], PBO 1%,
arbiter-confirmed, surfaced by the surrogate index (t~6). Shipped (default 0.35 + stored). Flagless
golden master now:
  CHAMPIONSHIPS: 42.3%  (random 6.3%)  |  playoffs: 97%   (n=150, 1999-2024)
Pre-edge base ~38.5% is reproduced by `--consensus-blend 0 --bench-discount 0.25`.
